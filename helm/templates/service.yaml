apiVersion: v1
kind: Service
metadata:
  name: {{ include "insightforge.fullname" . }}
  labels:
    {{- include "insightforge.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.app.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insightforge.selectorLabels" . | nindent 4 }}
